```dart
import 'package:flutter/material.dart';

void main() {
  runApp(MainPage());
}
//创建一个类继承StatelessWidget
//需要重写build方法，返回一个Widget。
//无状态组件单纯用于展示数据，没有任何用户交互操作。
//快捷键 输入st 然后选中Futter Stateless Widget
class MainPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp();
  }
  
}
```