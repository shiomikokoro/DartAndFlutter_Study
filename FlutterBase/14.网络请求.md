1. Dio基本使用

   1. 在项目根目录终端输入`flutter pub add dio`
   2. 基础使用 `Dio().get(url).then((res){}).catchError((err){})`
   3. 何时使用，一般在初始化状态 `initState` 中获取网络数据
2. 封装Dio插件

   1. ```dart
      class DioUtils{
        final Dio _dio = Dio();
        DioUtils(){
          //做些基础操作
          //配置基础地址和超时时间
          _dio.options..baseUrl = "https://geek.itheima.net/v1_0/channels"
          ..connectTimeout = Duration(seconds: 10)//连接超时
          ..sendTimeout = Duration(seconds: 10)//发送超时
          ..receiveTimeout = Duration(seconds: 10);//接收超时
          //拦截器
          _addInterceptor();
        }
        void _addInterceptor(){
          _dio.interceptors.add(InterceptorsWrapper(
            //请求拦截
            //放至下一个拦截 handler.next(requestOptions);
            //拦截 handler.reject(error);
            onRequest: (options, handler) {
              handler.next(options);
            },
            //响应拦截
            onResponse: (response, handler) {
              //http状态码 成功:2xx 缓存异常:3xx 请求参数异常:4xx 服务器异常:5xx
              if(response.statusCode! >=200 && response.statusCode!<300){
                handler.next(response);
                return;
              }
              handler.reject(DioException(requestOptions: response.requestOptions));
      
            },
            //错误拦截
            onError: (error, handler) {
              handler.reject(error);
            },
          ));
        }
        get(String url,{Map<String,dynamic>? params}){
          return _dio.get(url,queryParameters: params);
        }
      }
      ```
3. web端跨域

   1. 很遗憾，并不存在类似proxy办法再生产环境跨域访问，只能后端加CORS。
   2. 前端在开发环境中可以对 `flutter[环境目录]/packages/flutter_tools/lib/src/web/chrome.dart` 中 `ChromiumLauncher` 添加启动参数 `"--disable-web-security"` 来跳过CORS检查（危险操作）。
      1. 然后删除`flutter[环境目录]/bin/cache/` 下的 `flutter_tools.snaphot` 与 `flutter_tools.stamp` 快照
      2. 最后在项目目录 重新运行项目 `flutter doctor -v`